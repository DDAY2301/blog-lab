# Guardian Final Notes

This branch is the final production-guardian upgrade branch.

The merge target is `main`. The new guardrails activate after merge because GitHub Actions workflows are read from the default branch.
